{
  "name": "@grainrigi/date-reviver",
  "version": "1.0.1",
  "description": "JSON Date Automatic Parser (Reviver)",
  "main": "dist-cjs/index.js",
  "module": "dist-esm/index.js",
  "types": "dist-cjs/index.d.ts",
  "scripts": {
    "build": "npm-run-all -s build:cjs build:esm",
    "build:cjs": "tsc --project . --module commonjs --outDir ./dist-cjs",
    "build:esm": "tsc --project . --module es6 --outDir ./dist-esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "README.md",
    "dist-cjs",
    "dist-esm"
  ],
  "author": "grainrigi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/grainrigi/date-reviver"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^3.9.3"
  }
}
